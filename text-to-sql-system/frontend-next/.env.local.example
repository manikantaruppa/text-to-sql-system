# Optional: point Next.js proxy at a remote backend
BACKEND_API_URL=http://localhost:8000

# Optional: bypass Next.js proxy and call backend directly (not recommended for CORS)
# NEXT_PUBLIC_API_URL=http://localhost:8000
